#include <stdio.h>
 
int main() {
 
    int NUM;
    
    while (NUM != 2002){
        scanf("%d",&NUM);
        
        if(NUM == 2002){
            printf("Acesso Permitido\n");
        }else{
              printf("Senha Invalida");
        }
    }
    
    return 0;
}
